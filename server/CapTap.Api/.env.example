DATABASE_CONNECTION=Host=localhost;Port=5432;Database=captapdb;Username=captap;Password=captappassword
# Generate a strong secret (32+ chars). Never commit real production values.
# openssl rand -base64 48
JWT_SECRET=dev-only-change-me-to-a-long-random-secret
ASPNETCORE_URLS=http://localhost:5001

# Email: Mock (local) or Smtp (staging/production)
EMAIL_PROVIDER=Mock
EMAIL_HOST=
EMAIL_PORT=587
EMAIL_USERNAME=
EMAIL_PASSWORD=
EMAIL_FROM=
EMAIL_FROM_NAME=CapTap
EMAIL_USE_SSL=true
EMAIL_APP_BASE_URL=http://localhost:8081
